extends base

prepend window_title
  
  = page.title


append content
  article(class=page.layout)
    h2= page.title
    div.meta
      include _partial/article_meta

      +date(page.date)

      +category(page.categories)
    
    != page.content
    p
      != markdown(page.rumor)
    p
      != markdown(page.validated)

  include _partial/article_pagination